def HEAD(field) { 
    if (field=="I_E_PORT") {return entry.?entryHead.?iEPort;}
    else if (field=="DECL_PORT") {return entry.?entryHead.?declPort;}
};

def LIST(field) { 
    if (field=="G_NO") {return entry.?entryList[entry.goodsIndex].?gNo;}
};